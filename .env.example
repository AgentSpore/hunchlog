# HunchLog backend configuration
# Path to the sqlite database file.
DB_PATH=hunchlog.db
# CORS origins (comma-separated). "*" allows any origin.
CORS_ORIGINS=*
# Directory of the static frontend bundle, mounted at "/".
FRONTEND_DIR=frontend
